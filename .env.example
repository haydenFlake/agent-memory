# Storage
DATA_DIR=./data

# Memory System Configuration
DECAY_RATE=0.995
REFLECTION_THRESHOLD=15
CONSOLIDATION_INTERVAL=86400000
MERGE_SIMILARITY_THRESHOLD=0.85
PRUNE_AGE_DAYS=90

# Retrieval Weights
WEIGHT_RECENCY=0.4
WEIGHT_IMPORTANCE=0.3
WEIGHT_RELEVANCE=0.3

# Embedding
EMBEDDING_MODEL=Xenova/all-MiniLM-L6-v2
EMBEDDING_DIMENSIONS=384

# AI Services (optional — Claude Haiku for importance scoring + reflections)
# If absent, importance defaults to 0.5 and reflections are disabled
ANTHROPIC_API_KEY=sk-ant-...

# Logging
LOG_LEVEL=info
